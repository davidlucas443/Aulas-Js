import { StyleSheet  } from "react-native";
import { TEXT_COLOR_DAR, TEXT_COLOR_DISABLED, TEXT_COLOR_MEDIUM, BORDER_COLOR_LIGHT, BACKGROUND_COLOR_INPUT, BACKGROUND_COLOR_WHITE } from "../constants/style";

export const styles = StyleSheet.create({
    taskItem: {
        backgroundColor: BACKGROUND_COLOR_WHITE,
        paddingVertical: 15,
        paddingHorizontal: 20,
}
});